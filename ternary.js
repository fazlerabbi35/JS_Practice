    // Ternary Operator **********

let n = 12
let ans = (n % 2 == 0) ? 'Even Number' : 'Odd Number'
console.log(ans)


let num = 22
let req = (num >= 18) ? 'You are accessable' : 'Something wrong'
console.log(req)


let price = 120
let take = (price > 150) ? `I'll take it.` : `It's too much. I can't effort it.`
console.log(take)


let taka = 20
let khabo = (taka >= 30) ? "Ami khabo" : "Ami khabo na."
console.log(khabo)


let time = 4.30
let khala = (time >= 5.00) ? "Ami khalta jabo" : "Ami r o aktu pora jabo."
console.log(khala)


let Arohi = 'single'
let ami = (Arohi === 'single') ? "Flatt kora jaba" : "kotha bolbo na."
console.log(ami)

